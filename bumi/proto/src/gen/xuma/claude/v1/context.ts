// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: xuma/claude/v1/context.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "xuma.claude.v1";

/** Context passed to matchers for Claude Code hook evaluation. */
export interface HookContext {
  /** Hook event type. */
  eventType: string;
  /** Tool being invoked (for tool-related hooks). */
  toolName: string;
  /** Tool arguments as key-value pairs. */
  toolArgs: { [key: string]: string };
  /** Current session ID. */
  sessionId: string;
  /** Current working directory. */
  cwd: string;
  /** Git branch (if in a git repo). */
  gitBranch: string;
}

export interface HookContext_ToolArgsEntry {
  key: string;
  value: string;
}

/** Action to allow the tool use to proceed. */
export interface AllowAction {
  /** Optional message to display. */
  message: string;
}

/** Action to block the tool use. */
export interface BlockAction {
  /** Reason for blocking (displayed to user). */
  reason: string;
}

/** Action to modify the tool use. */
export interface ModifyAction {
  /** Modified tool arguments. */
  modifiedArgs: { [key: string]: string };
  /** Optional message explaining the modification. */
  message: string;
}

export interface ModifyAction_ModifiedArgsEntry {
  key: string;
  value: string;
}

function createBaseHookContext(): HookContext {
  return { eventType: "", toolName: "", toolArgs: {}, sessionId: "", cwd: "", gitBranch: "" };
}

export const HookContext: MessageFns<HookContext> = {
  encode(message: HookContext, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventType !== "") {
      writer.uint32(10).string(message.eventType);
    }
    if (message.toolName !== "") {
      writer.uint32(18).string(message.toolName);
    }
    globalThis.Object.entries(message.toolArgs).forEach(([key, value]: [string, string]) => {
      HookContext_ToolArgsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.sessionId !== "") {
      writer.uint32(34).string(message.sessionId);
    }
    if (message.cwd !== "") {
      writer.uint32(42).string(message.cwd);
    }
    if (message.gitBranch !== "") {
      writer.uint32(50).string(message.gitBranch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HookContext {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHookContext();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.toolName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = HookContext_ToolArgsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.toolArgs[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.cwd = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.gitBranch = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HookContext {
    return {
      eventType: isSet(object.eventType)
        ? globalThis.String(object.eventType)
        : isSet(object.event_type)
        ? globalThis.String(object.event_type)
        : "",
      toolName: isSet(object.toolName)
        ? globalThis.String(object.toolName)
        : isSet(object.tool_name)
        ? globalThis.String(object.tool_name)
        : "",
      toolArgs: isObject(object.toolArgs)
        ? (globalThis.Object.entries(object.toolArgs) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.tool_args)
        ? (globalThis.Object.entries(object.tool_args) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      cwd: isSet(object.cwd) ? globalThis.String(object.cwd) : "",
      gitBranch: isSet(object.gitBranch)
        ? globalThis.String(object.gitBranch)
        : isSet(object.git_branch)
        ? globalThis.String(object.git_branch)
        : "",
    };
  },

  toJSON(message: HookContext): unknown {
    const obj: any = {};
    if (message.eventType !== "") {
      obj.eventType = message.eventType;
    }
    if (message.toolName !== "") {
      obj.toolName = message.toolName;
    }
    if (message.toolArgs) {
      const entries = globalThis.Object.entries(message.toolArgs) as [string, string][];
      if (entries.length > 0) {
        obj.toolArgs = {};
        entries.forEach(([k, v]) => {
          obj.toolArgs[k] = v;
        });
      }
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.cwd !== "") {
      obj.cwd = message.cwd;
    }
    if (message.gitBranch !== "") {
      obj.gitBranch = message.gitBranch;
    }
    return obj;
  },

  create(base?: DeepPartial<HookContext>): HookContext {
    return HookContext.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HookContext>): HookContext {
    const message = createBaseHookContext();
    message.eventType = object.eventType ?? "";
    message.toolName = object.toolName ?? "";
    message.toolArgs = (globalThis.Object.entries(object.toolArgs ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.sessionId = object.sessionId ?? "";
    message.cwd = object.cwd ?? "";
    message.gitBranch = object.gitBranch ?? "";
    return message;
  },
};

function createBaseHookContext_ToolArgsEntry(): HookContext_ToolArgsEntry {
  return { key: "", value: "" };
}

export const HookContext_ToolArgsEntry: MessageFns<HookContext_ToolArgsEntry> = {
  encode(message: HookContext_ToolArgsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HookContext_ToolArgsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHookContext_ToolArgsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HookContext_ToolArgsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: HookContext_ToolArgsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<HookContext_ToolArgsEntry>): HookContext_ToolArgsEntry {
    return HookContext_ToolArgsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HookContext_ToolArgsEntry>): HookContext_ToolArgsEntry {
    const message = createBaseHookContext_ToolArgsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseAllowAction(): AllowAction {
  return { message: "" };
}

export const AllowAction: MessageFns<AllowAction> = {
  encode(message: AllowAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AllowAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAllowAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AllowAction {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: AllowAction): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<AllowAction>): AllowAction {
    return AllowAction.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AllowAction>): AllowAction {
    const message = createBaseAllowAction();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseBlockAction(): BlockAction {
  return { reason: "" };
}

export const BlockAction: MessageFns<BlockAction> = {
  encode(message: BlockAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== "") {
      writer.uint32(10).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BlockAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBlockAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BlockAction {
    return { reason: isSet(object.reason) ? globalThis.String(object.reason) : "" };
  },

  toJSON(message: BlockAction): unknown {
    const obj: any = {};
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<BlockAction>): BlockAction {
    return BlockAction.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BlockAction>): BlockAction {
    const message = createBaseBlockAction();
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseModifyAction(): ModifyAction {
  return { modifiedArgs: {}, message: "" };
}

export const ModifyAction: MessageFns<ModifyAction> = {
  encode(message: ModifyAction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    globalThis.Object.entries(message.modifiedArgs).forEach(([key, value]: [string, string]) => {
      ModifyAction_ModifiedArgsEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModifyAction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModifyAction();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = ModifyAction_ModifiedArgsEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.modifiedArgs[entry1.key] = entry1.value;
          }
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModifyAction {
    return {
      modifiedArgs: isObject(object.modifiedArgs)
        ? (globalThis.Object.entries(object.modifiedArgs) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.modified_args)
        ? (globalThis.Object.entries(object.modified_args) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: ModifyAction): unknown {
    const obj: any = {};
    if (message.modifiedArgs) {
      const entries = globalThis.Object.entries(message.modifiedArgs) as [string, string][];
      if (entries.length > 0) {
        obj.modifiedArgs = {};
        entries.forEach(([k, v]) => {
          obj.modifiedArgs[k] = v;
        });
      }
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<ModifyAction>): ModifyAction {
    return ModifyAction.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ModifyAction>): ModifyAction {
    const message = createBaseModifyAction();
    message.modifiedArgs = (globalThis.Object.entries(object.modifiedArgs ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseModifyAction_ModifiedArgsEntry(): ModifyAction_ModifiedArgsEntry {
  return { key: "", value: "" };
}

export const ModifyAction_ModifiedArgsEntry: MessageFns<ModifyAction_ModifiedArgsEntry> = {
  encode(message: ModifyAction_ModifiedArgsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModifyAction_ModifiedArgsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModifyAction_ModifiedArgsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModifyAction_ModifiedArgsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ModifyAction_ModifiedArgsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<ModifyAction_ModifiedArgsEntry>): ModifyAction_ModifiedArgsEntry {
    return ModifyAction_ModifiedArgsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ModifyAction_ModifiedArgsEntry>): ModifyAction_ModifiedArgsEntry {
    const message = createBaseModifyAction_ModifiedArgsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: xuma/claude/v1/inputs.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "xuma.claude.v1";

/**
 * Extracts the hook event type.
 * Values: "PreToolUse", "PostToolUse", "Stop", "SessionStart", etc.
 */
export interface EventTypeInput {
}

/**
 * Extracts the tool name being invoked.
 * Values: "Bash", "Read", "Write", "Edit", etc.
 */
export interface ToolNameInput {
}

/** Extracts a tool argument value by name. */
export interface ToolArgInput {
  /** Argument name to extract. */
  name: string;
}

/** Extracts the session ID. */
export interface SessionIdInput {
}

/** Extracts the current working directory. */
export interface CwdInput {
}

/** Extracts the git branch name (if in a git repo). */
export interface GitBranchInput {
}

function createBaseEventTypeInput(): EventTypeInput {
  return {};
}

export const EventTypeInput: MessageFns<EventTypeInput> = {
  encode(_: EventTypeInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventTypeInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventTypeInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): EventTypeInput {
    return {};
  },

  toJSON(_: EventTypeInput): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<EventTypeInput>): EventTypeInput {
    return EventTypeInput.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<EventTypeInput>): EventTypeInput {
    const message = createBaseEventTypeInput();
    return message;
  },
};

function createBaseToolNameInput(): ToolNameInput {
  return {};
}

export const ToolNameInput: MessageFns<ToolNameInput> = {
  encode(_: ToolNameInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolNameInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolNameInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ToolNameInput {
    return {};
  },

  toJSON(_: ToolNameInput): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ToolNameInput>): ToolNameInput {
    return ToolNameInput.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ToolNameInput>): ToolNameInput {
    const message = createBaseToolNameInput();
    return message;
  },
};

function createBaseToolArgInput(): ToolArgInput {
  return { name: "" };
}

export const ToolArgInput: MessageFns<ToolArgInput> = {
  encode(message: ToolArgInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolArgInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolArgInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolArgInput {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: ToolArgInput): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create(base?: DeepPartial<ToolArgInput>): ToolArgInput {
    return ToolArgInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ToolArgInput>): ToolArgInput {
    const message = createBaseToolArgInput();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseSessionIdInput(): SessionIdInput {
  return {};
}

export const SessionIdInput: MessageFns<SessionIdInput> = {
  encode(_: SessionIdInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionIdInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionIdInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SessionIdInput {
    return {};
  },

  toJSON(_: SessionIdInput): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<SessionIdInput>): SessionIdInput {
    return SessionIdInput.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SessionIdInput>): SessionIdInput {
    const message = createBaseSessionIdInput();
    return message;
  },
};

function createBaseCwdInput(): CwdInput {
  return {};
}

export const CwdInput: MessageFns<CwdInput> = {
  encode(_: CwdInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CwdInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCwdInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CwdInput {
    return {};
  },

  toJSON(_: CwdInput): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CwdInput>): CwdInput {
    return CwdInput.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CwdInput>): CwdInput {
    const message = createBaseCwdInput();
    return message;
  },
};

function createBaseGitBranchInput(): GitBranchInput {
  return {};
}

export const GitBranchInput: MessageFns<GitBranchInput> = {
  encode(_: GitBranchInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitBranchInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitBranchInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GitBranchInput {
    return {};
  },

  toJSON(_: GitBranchInput): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GitBranchInput>): GitBranchInput {
    return GitBranchInput.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GitBranchInput>): GitBranchInput {
    const message = createBaseGitBranchInput();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

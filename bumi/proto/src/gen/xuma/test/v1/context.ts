// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: xuma/test/v1/context.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "xuma.test.v1";

/**
 * Test context passed to matchers during conformance testing.
 * Provides data that inputs extract from.
 */
export interface TestContext {
  /** Simple string value (for StringInput). */
  stringValue: string;
  /** Map of string values (for MapInput). */
  mapValues: { [key: string]: string };
  /** Optional bytes value for binary matching. */
  bytesValue: Uint8Array;
}

export interface TestContext_MapValuesEntry {
  key: string;
  value: string;
}

function createBaseTestContext(): TestContext {
  return { stringValue: "", mapValues: {}, bytesValue: new Uint8Array(0) };
}

export const TestContext: MessageFns<TestContext> = {
  encode(message: TestContext, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stringValue !== "") {
      writer.uint32(10).string(message.stringValue);
    }
    globalThis.Object.entries(message.mapValues).forEach(([key, value]: [string, string]) => {
      TestContext_MapValuesEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.bytesValue.length !== 0) {
      writer.uint32(26).bytes(message.bytesValue);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TestContext {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestContext();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stringValue = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = TestContext_MapValuesEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.mapValues[entry2.key] = entry2.value;
          }
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.bytesValue = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TestContext {
    return {
      stringValue: isSet(object.stringValue)
        ? globalThis.String(object.stringValue)
        : isSet(object.string_value)
        ? globalThis.String(object.string_value)
        : "",
      mapValues: isObject(object.mapValues)
        ? (globalThis.Object.entries(object.mapValues) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.map_values)
        ? (globalThis.Object.entries(object.map_values) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      bytesValue: isSet(object.bytesValue)
        ? bytesFromBase64(object.bytesValue)
        : isSet(object.bytes_value)
        ? bytesFromBase64(object.bytes_value)
        : new Uint8Array(0),
    };
  },

  toJSON(message: TestContext): unknown {
    const obj: any = {};
    if (message.stringValue !== "") {
      obj.stringValue = message.stringValue;
    }
    if (message.mapValues) {
      const entries = globalThis.Object.entries(message.mapValues) as [string, string][];
      if (entries.length > 0) {
        obj.mapValues = {};
        entries.forEach(([k, v]) => {
          obj.mapValues[k] = v;
        });
      }
    }
    if (message.bytesValue.length !== 0) {
      obj.bytesValue = base64FromBytes(message.bytesValue);
    }
    return obj;
  },

  create(base?: DeepPartial<TestContext>): TestContext {
    return TestContext.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TestContext>): TestContext {
    const message = createBaseTestContext();
    message.stringValue = object.stringValue ?? "";
    message.mapValues = (globalThis.Object.entries(object.mapValues ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.bytesValue = object.bytesValue ?? new Uint8Array(0);
    return message;
  },
};

function createBaseTestContext_MapValuesEntry(): TestContext_MapValuesEntry {
  return { key: "", value: "" };
}

export const TestContext_MapValuesEntry: MessageFns<TestContext_MapValuesEntry> = {
  encode(message: TestContext_MapValuesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TestContext_MapValuesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTestContext_MapValuesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TestContext_MapValuesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: TestContext_MapValuesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<TestContext_MapValuesEntry>): TestContext_MapValuesEntry {
    return TestContext_MapValuesEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TestContext_MapValuesEntry>): TestContext_MapValuesEntry {
    const message = createBaseTestContext_MapValuesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

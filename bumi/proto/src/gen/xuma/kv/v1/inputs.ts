// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: xuma/kv/v1/inputs.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "xuma.kv.v1";

/**
 * Reads one entry from a string-to-string map.
 *
 * The key-value domain is the simplest matching domain x.uma has, and the one
 * the `rumi` CLI uses by default. This message used to live in `xuma.test.v1`
 * as `StringInput`, whose only field was named `value` while the code used it
 * as a lookup key — a config that read `key:` against a schema that said
 * `value`. The concept was never "test", and it was never a value.
 */
export interface MapInput {
  /**
   * Which entry to read. Required in practice: an empty key reads nothing, so
   * the predicate is always false. proto3 cannot express that, so the
   * constructor rejects it.
   */
  key: string;
}

function createBaseMapInput(): MapInput {
  return { key: "" };
}

export const MapInput: MessageFns<MapInput> = {
  encode(message: MapInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MapInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMapInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MapInput {
    return { key: isSet(object.key) ? globalThis.String(object.key) : "" };
  },

  toJSON(message: MapInput): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create(base?: DeepPartial<MapInput>): MapInput {
    return MapInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MapInput>): MapInput {
    const message = createBaseMapInput();
    message.key = object.key ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

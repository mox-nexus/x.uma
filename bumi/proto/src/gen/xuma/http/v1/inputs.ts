// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: xuma/http/v1/inputs.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "xuma.http.v1";

/** Extracts an HTTP header value by name. */
export interface HeaderInput {
  /** Header name to extract (case-insensitive). */
  name: string;
}

/** Extracts the HTTP request path. */
export interface PathInput {
}

/** Extracts the HTTP request method. */
export interface MethodInput {
}

/** Extracts a query parameter value by name. */
export interface QueryParamInput {
  /** Query parameter name. */
  name: string;
}

/**
 * Extracts the HTTP authority — the :authority pseudo-header, or Host in
 * HTTP/1. Named for what the code registers: this message was `HostInput`
 * while the registry advertised `xuma.http.v1.AuthorityInput`, so the schema
 * and the type URL named different things.
 */
export interface AuthorityInput {
}

/** Extracts the HTTP scheme (http/https). */
export interface SchemeInput {
}

function createBaseHeaderInput(): HeaderInput {
  return { name: "" };
}

export const HeaderInput: MessageFns<HeaderInput> = {
  encode(message: HeaderInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeaderInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeaderInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HeaderInput {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: HeaderInput): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create(base?: DeepPartial<HeaderInput>): HeaderInput {
    return HeaderInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HeaderInput>): HeaderInput {
    const message = createBaseHeaderInput();
    message.name = object.name ?? "";
    return message;
  },
};

function createBasePathInput(): PathInput {
  return {};
}

export const PathInput: MessageFns<PathInput> = {
  encode(_: PathInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PathInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePathInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): PathInput {
    return {};
  },

  toJSON(_: PathInput): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<PathInput>): PathInput {
    return PathInput.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<PathInput>): PathInput {
    const message = createBasePathInput();
    return message;
  },
};

function createBaseMethodInput(): MethodInput {
  return {};
}

export const MethodInput: MessageFns<MethodInput> = {
  encode(_: MethodInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MethodInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMethodInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MethodInput {
    return {};
  },

  toJSON(_: MethodInput): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MethodInput>): MethodInput {
    return MethodInput.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MethodInput>): MethodInput {
    const message = createBaseMethodInput();
    return message;
  },
};

function createBaseQueryParamInput(): QueryParamInput {
  return { name: "" };
}

export const QueryParamInput: MessageFns<QueryParamInput> = {
  encode(message: QueryParamInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamInput {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: QueryParamInput): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamInput>): QueryParamInput {
    return QueryParamInput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamInput>): QueryParamInput {
    const message = createBaseQueryParamInput();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseAuthorityInput(): AuthorityInput {
  return {};
}

export const AuthorityInput: MessageFns<AuthorityInput> = {
  encode(_: AuthorityInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthorityInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthorityInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): AuthorityInput {
    return {};
  },

  toJSON(_: AuthorityInput): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<AuthorityInput>): AuthorityInput {
    return AuthorityInput.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<AuthorityInput>): AuthorityInput {
    const message = createBaseAuthorityInput();
    return message;
  },
};

function createBaseSchemeInput(): SchemeInput {
  return {};
}

export const SchemeInput: MessageFns<SchemeInput> = {
  encode(_: SchemeInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SchemeInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSchemeInput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SchemeInput {
    return {};
  },

  toJSON(_: SchemeInput): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<SchemeInput>): SchemeInput {
    return SchemeInput.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SchemeInput>): SchemeInput {
    const message = createBaseSchemeInput();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: xuma/core/v1/action.proto
# plugin: python-betterproto
from dataclasses import dataclass
from typing import Dict

import betterproto


@dataclass
class NamedAction(betterproto.Message):
    """
    Generic action that returns a named result. Used as a base action type
    across domains.
    """

    # Action identifier returned on match.
    name: str = betterproto.string_field(1)
    # Optional metadata attached to the action.
    metadata: Dict[str, str] = betterproto.map_field(
        2, betterproto.TYPE_STRING, betterproto.TYPE_STRING
    )

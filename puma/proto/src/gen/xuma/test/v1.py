# Generated by the protocol buffer compiler.  DO NOT EDIT!
# sources: xuma/test/v1/context.proto, xuma/test/v1/inputs.proto
# plugin: python-betterproto
from dataclasses import dataclass
from typing import Dict

import betterproto


@dataclass
class TestContext(betterproto.Message):
    """
    Test context passed to matchers during conformance testing. Provides data
    that inputs extract from.
    """

    # Simple string value (for StringInput).
    string_value: str = betterproto.string_field(1)
    # Map of string values (for MapInput).
    map_values: Dict[str, str] = betterproto.map_field(
        2, betterproto.TYPE_STRING, betterproto.TYPE_STRING
    )
    # Optional bytes value for binary matching.
    bytes_value: bytes = betterproto.bytes_field(3)


@dataclass
class StringInput(betterproto.Message):
    """
    Simple string input for conformance testing. Extracts a single string value
    from test context.
    """

    # The string value to match against.
    value: str = betterproto.string_field(1)


@dataclass
class MapInput(betterproto.Message):
    """
    Map-based input for conformance testing. Extracts a value from a string map
    by key (like headers).
    """

    # Key to extract from the map.
    key: str = betterproto.string_field(1)


@dataclass
class ConstantInput(betterproto.Message):
    """
    Input that always returns the specified value. Useful for testing matcher
    logic in isolation.
    """

    # The constant value to return.
    value: str = betterproto.string_field(1)
